// NUBEMO — Area Paziente / runtime Supabase-first
(() => {
  'use strict';
  const services = window.nubemoPatientServices;
  const app = document.getElementById('app');
  const state = { page:'home', context:null, diary:[], selfMeasures:[], proMeasures:[], documents:[], plans:[], planLinks:[], appointments:[], privacy:{documents:[],acceptances:[]}, editDiaryId:null, editSelfMeasureId:null, trendDays:7, showMovingAverage:false };

  const esc=(v='')=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
  const isoToday=()=>{const d=new Date(),z=d.getTimezoneOffset()*60000;return new Date(d-z).toISOString().slice(0,10);};
  const fmtDate=v=>v?new Date(String(v).slice(0,10)+'T12:00:00').toLocaleDateString('it-IT'):'—';
  const kg=v=>v===null||v===undefined||v===''?'—':`${Number(v).toFixed(1).replace('.',',')} kg`;
  const cm=v=>v===null||v===undefined||v===''?'—':`${Number(v).toFixed(1).replace('.',',')} cm`;
  const nullableNumber=v=>{const s=String(v??'').trim().replace(',','.');return s===''?null:Number(s);};
  const textOrNull=v=>{const s=String(v??'').trim();return s||null;};
  const active=()=>!!state.context?.activePathway;

  const NUBEMO_DAILY_QUOTES=['La costanza batte la giornata perfetta.','Oggi fai il tuo. Domani si vedrà.','La bilancia registra il peso, non tutto il lavoro che c’è dietro.','Non tutte le giornate devono essere memorabili. Devono solo esistere.','Un risultato lento resta un risultato.','Oggi niente imprese: fai semplicemente quello che avevi programmato.','La direzione conta più della velocità.','Una giornata storta non cancella quelle giuste.','Il corpo non legge il calendario: dagli tempo.','Non serve vincere ogni giorno per arrivare lontano.','La bilancia oggi ha un’opinione. Non necessariamente ragione.','Il piano perfetto? Quello che riesci a seguire anche martedì.','Se oggi va tutto liscio, bene. Se no, domani esiste ancora.','Il peso fa su e giù. Tu prova ad andare avanti.','NUBEMO non giudica. Al massimo fa i conti.','Una buona abitudine è noiosa solo finché non mostra i risultati.','Il percorso non richiede effetti speciali.','Oggi puoi anche non sentirti motivato. Il piano funziona lo stesso.','La perfezione è sopravvalutata. La continuità molto meno.','La bilancia non assegna voti.','Guarda la tendenza, non il singolo numero.','Quello che fai spesso conta più di quello che fai ogni tanto.','Un pasto non decide il percorso. Nemmeno una giornata.','Le abitudini lavorano anche quando non fanno rumore.','Misurare serve a capire, non a giudicare.','Il dato di oggi è un dato, non una sentenza.','Fai spazio ai risultati senza pretendere che arrivino in orario.','La routine è meno spettacolare della motivazione, ma dura di più.','Ripetere le cose semplici è una strategia.','Il cambiamento vero spesso sembra normale mentre accade.','Ci sono giorni in cui avanzare significa semplicemente non mollare.','Se oggi è difficile, riduci l’ambizione, non l’impegno.','Una giornata complicata non richiede una punizione domani.','Puoi essere stanco e continuare comunque con gentilezza.','Non devi recuperare ieri. Devi vivere bene oggi.','Quando il risultato tarda, il lavoro fatto non scompare.','Anche una settimana piatta fa parte di una linea che può scendere.','Non trasformare un numero inatteso in una storia che non conosci.','Il corpo può trattenere acqua. Tu non trattenere il buon senso.','Oggi potrebbe non essere il giorno del risultato. Può essere quello che lo prepara.','Ripartire non significa ricominciare da zero.','Hai già imparato cose che la prima volta non sapevi.','Un’interruzione cambia il ritmo, non necessariamente la destinazione.','Il prossimo passo non deve compensare nulla: deve solo essere quello giusto.','Ricominciare bene vale più che ricominciare forte.','Non aspettare il lunedì per fare una scelta utile.','Una deviazione non obbliga a cambiare strada.','Il percorso continua dal punto in cui sei, non da quello in cui vorresti essere.','Non serve cancellare una giornata: basta non trasformarla in una settimana.','Ogni ritorno alla routine è già progresso.','Concediti il tempo di accorgerti di quanto sei cambiato.','A volte il risultato arriva prima nelle abitudini e poi nei numeri.','La pazienza non è aspettare: è continuare mentre aspetti.','Non tutto ciò che migliora si vede subito.','Il lungo periodo è fatto di giornate molto normali.','Essere soddisfatti non significa aver finito.','Nota anche quello che oggi fai senza fatica e mesi fa sembrava difficile.','I progressi piccoli diventano evidenti quando smetti di guardarli con il microscopio.','Il tempo passa comunque. Tanto vale usarlo dalla tua parte.','Il risultato migliore è quello che riesci a mantenere.','Bevi, mangia, muoviti, dormi. A volte la strategia è meno misteriosa del previsto.','Fai la prossima scelta utile, non le prossime cento.','Se hai un piano, oggi devi solo seguirne un pezzo.','Controlla ciò che puoi controllare e lascia respirare il resto.','Prima la routine, poi le rifiniture.','Non cambiare strategia per colpa di un singolo dato.','Annotare bene oggi aiuta a capire meglio domani.','La regolarità rende leggibili anche i numeri.','Se qualcosa non funziona, si corregge. Non si processa.','Il percorso è anche imparare come risponde il tuo corpo.','Fatto è meglio di perfetto.','Oggi basta esserci.','Continua.','Un giorno alla volta è una misura sorprendentemente efficace.','Niente magie. Solo tempo e scelte ripetute.','Respira. Il trend ha più memoria della bilancia di stamattina.','Non devi dimostrare niente a nessuno.','Il prossimo dato arriverà. Nel frattempo vivi.','Meno rumore, più continuità.','Domani non ha bisogno di un oggi perfetto.'];
  function dailyMotivationalQuote(date=new Date()){const start=new Date(2026,0,1),day=new Date(date.getFullYear(),date.getMonth(),date.getDate());const i=((Math.floor((day-start)/86400000)%NUBEMO_DAILY_QUOTES.length)+NUBEMO_DAILY_QUOTES.length)%NUBEMO_DAILY_QUOTES.length;return NUBEMO_DAILY_QUOTES[i];}

  function weighted(){return state.diary.filter(x=>x.weight_kg!=null&&Number.isFinite(Number(x.weight_kg))).map(x=>({date:x.entry_date,weight:Number(x.weight_kg)})).sort((a,b)=>a.date.localeCompare(b.date));}
  function filteredByDays(items,days){if(!days||!items.length)return items;const last=new Date(items.at(-1).date+'T12:00:00'),start=new Date(last);start.setDate(start.getDate()-(days-1));return items.filter(x=>new Date(x.date+'T12:00:00')>=start);}
  function movingAverageSeries(items){return items.map((x,i)=>{const slice=items.slice(Math.max(0,i-6),i+1);return {date:x.date,weight:slice.reduce((s,v)=>s+Number(v.weight),0)/slice.length};});}
  function chartHtml(items,days,showAverage=false){
    const w=filteredByDays(items,days); if(!w.length)return '<p class="muted">Nessun peso registrato.</p>';
    const avg=movingAverageSeries(items).filter(x=>w.some(y=>y.date===x.date));
    const vals=w.map(x=>x.weight),rawMin=Math.min(...vals),rawMax=Math.max(...vals),pad=Math.max(.5,(rawMax-rawMin)*.18);
    let min=Math.floor((rawMin-pad)*2)/2,max=Math.ceil((rawMax+pad)*2)/2;if(max===min)max=min+1;const range=max-min;
    const left=31,right=156,top=7,bottom=60,dateY=75;
    const xFor=date=>{const i=w.findIndex(x=>x.date===date);return w.length===1?(left+right)/2:left+i/(w.length-1)*(right-left);};
    const yFor=v=>bottom-((v-min)/range)*(bottom-top);
    const pts=w.map(x=>`${xFor(x.date)},${yFor(x.weight)}`).join(' '),avgPts=avg.map(x=>`${xFor(x.date)},${yFor(x.weight)}`).join(' ');
    const ticks=5,grid=Array.from({length:ticks},(_,i)=>{const v=min+(range*i/(ticks-1)),y=yFor(v);return `<line x1="${left}" y1="${y}" x2="${right}" y2="${y}" class="chart-grid"/><text x="${left-4}" y="${y}" text-anchor="end" dominant-baseline="middle" class="chart-y-label">${v.toFixed(1).replace('.',',')}</text>`;}).join('');
    const startLabel=fmtDate(w[0].date),endLabel=fmtDate(w.at(-1).date);
    return `<div class="chart-wrap"><svg class="chart responsive-chart" viewBox="0 0 160 80" preserveAspectRatio="none">${grid}<line x1="${left}" y1="${top}" x2="${left}" y2="${bottom}" class="chart-axis"/><line x1="${left}" y1="${bottom}" x2="${right}" y2="${bottom}" class="chart-axis"/><polyline points="${pts}" class="chart-line" fill="none" vector-effect="non-scaling-stroke"/>${w.map(x=>`<circle cx="${xFor(x.date)}" cy="${yFor(x.weight)}" r="0.8" class="chart-point" vector-effect="non-scaling-stroke"/>`).join('')}${showAverage&&avgPts?`<polyline points="${avgPts}" class="chart-average" fill="none" vector-effect="non-scaling-stroke"/>`:''}<text x="${left}" y="${dateY}" text-anchor="start" class="chart-x-label">${esc(startLabel)}</text><text x="${right}" y="${dateY}" text-anchor="end" class="chart-x-label">${esc(endLabel)}</text></svg><span class="chart-unit-fixed">kg</span></div>`;
  }

  function latestDiaryWeight(){return weighted().at(-1)||null;}
  function nextAppointment(){const now=new Date();return state.appointments.filter(a=>new Date(a.starts_at)>=now&&a.status!=='cancelled').sort((a,b)=>new Date(a.starts_at)-new Date(b.starts_at))[0]||null;}
  function currentPlan(){const today=isoToday();return state.plans.filter(p=>p.status==='active'&&(!p.valid_from||p.valid_from<=today)&&(!p.valid_to||p.valid_to>=today)).sort((a,b)=>String(b.valid_from||'').localeCompare(String(a.valid_from||'')))[0]||null;}

  async function reloadAll(){const c=state.context;const [diary,selfMeasures,proMeasures,documents,plans,appointments,privacy]=await Promise.all([services.loadDiary(c.patient.id),services.loadSelfMeasurements(c.patient.id),services.loadProfessionalMeasurements(c.patient.id),services.loadDocuments(c.patient.id),services.loadPlans(c.patient.id),services.loadAppointments(c.patient.id),services.loadPrivacy(c.profile.id)]);const planLinks=await services.loadPlanDocuments(plans.map(p=>p.id));Object.assign(state,{diary,selfMeasures,proMeasures,documents,plans,appointments,privacy,planLinks});}
  function loading(label='Caricamento…'){app.innerHTML=`<section class="card"><p class="muted">${esc(label)}</p></section>`;}
  function errorMessage(error){console.error(error);alert(error?.message||'Operazione non completata.');}

  function homeHtml(){const c=state.context,last=latestDiaryWeight(),visit=nextAppointment(),plan=currentPlan(),w=weighted();const greeting=c.profile.first_name?`Ciao ${esc(c.profile.first_name)}`:'Il tuo percorso';return `<div class="hero-title"><div><div class="eyebrow">IL TUO PERCORSO, OGNI GIORNO</div><h1>${greeting}</h1></div></div>${active()?'':'<section class="card notice"><b>Percorso concluso</b><p class="muted">NUBEMO è in sola consultazione. Il tuo storico resta disponibile.</p></section>'}<section class="daily-quote" aria-label="Pensiero del giorno"><span class="daily-quote-label">PENSIERO DEL GIORNO</span><p>“${esc(dailyMotivationalQuote())}”</p></section><section class="card summary"><div class="section-head"><h2>Oggi</h2></div><div class="stats"><div><span>Ultimo peso registrato</span><b>${last?kg(last.weight):'—'}</b></div><div><span>Prossima visita</span><b>${visit?new Date(visit.starts_at).toLocaleString('it-IT',{dateStyle:'short',timeStyle:'short'}):'—'}</b></div><div><span>Piano alimentare</span><b>${plan?esc(plan.title):'—'}</b></div></div></section><section class="card chart-card"><div class="section-head"><h2>Ultimi 7 giorni</h2><span class="pill">Peso</span></div>${chartHtml(w,7,false)}</section><section class="card"><div class="section-head"><h2>Ultime giornate</h2>${active()?'<button class="primary compact" data-go="add">＋ Aggiungi</button>':''}</div>${state.diary.length?[...state.diary].reverse().slice(0,5).map(d=>`<div class="listitem" data-edit-diary="${d.id}"><span><b>${fmtDate(d.entry_date)}</b><small>${esc(d.breakfast||d.lunch||d.dinner||'Giornata registrata')}</small></span><b>${kg(d.weight_kg)}</b></div>`).join(''):'<p class="muted">Nessuna giornata registrata.</p>'}</section>`;}

  function diaryFormHtml(){if(!active())return `<section class="card"><h2>Diario</h2><p class="muted">Il percorso è concluso: puoi consultare lo storico, ma non aggiungere o modificare giornate.</p></section>`;const existing=state.editDiaryId?state.diary.find(x=>x.id===state.editDiaryId):null,d=existing||{entry_date:isoToday(),coffee:0};return `<div class="page-title"><button class="back" data-go="home">‹</button><div><div class="eyebrow">${existing?'REGISTRAZIONE':'NUOVA REGISTRAZIONE'}</div><h1>${existing?'Modifica giornata':'Aggiungi giornata'}</h1></div>${existing?'<button class="mini danger-text" id="deleteDiary">Elimina</button>':''}</div><section class="card form-card"><label>Data</label><input id="diaryDate" type="date" value="${esc(d.entry_date)}"><label>Peso (kg)</label><input id="diaryWeight" inputmode="decimal" value="${d.weight_kg??''}" placeholder="es. 115,6"><label>Acqua bevuta (litri)</label><input id="diaryWater" inputmode="decimal" value="${d.water??''}" placeholder="es. 1,5"><label>Caffè</label><input id="diaryCoffee" type="number" min="0" step="1" value="${d.coffee??0}"><label>Zucchero / dolcificante</label><input id="diarySweetener" value="${esc(d.sweetener||'')}"><label>🥐 Colazione</label><textarea id="diaryBreakfast">${esc(d.breakfast||'')}</textarea><label>🍎 Spuntino mattina</label><textarea id="diaryMorningSnack">${esc(d.morning_snack||'')}</textarea><label>🍝 Pranzo</label><textarea id="diaryLunch">${esc(d.lunch||'')}</textarea><label>🍎 Spuntino pomeriggio</label><textarea id="diaryAfternoonSnack">${esc(d.afternoon_snack||'')}</textarea><label>🍽️ Cena</label><textarea id="diaryDinner">${esc(d.dinner||'')}</textarea><label>🏃 Sport</label><textarea id="diarySport">${esc(d.sport||'')}</textarea><label>Note</label><textarea id="diaryNotes">${esc(d.notes||'')}</textarea><div class="form-actions"><button class="primary" id="saveDiary">${existing?'Aggiorna giornata':'Salva giornata'}</button>${existing?'<button class="secondary" id="duplicateDiary">⧉ Duplica giornata</button>':''}</div></section>`;}

  function trendHtml(){const all=weighted(),first=all[0],last=all.at(-1),delta=first&&last?last.weight-first.weight:null,rows=[...state.diary].reverse();return `<div class="hero-title"><div><div class="eyebrow">STATISTICHE</div><h1>Andamento</h1></div><button class="pdf-btn" id="exportDiaryPdf">PDF</button></div><section class="card chart-card"><div class="section-head"><h2>Peso</h2><label class="toggle"><input id="movingAverageToggle" type="checkbox" ${state.showMovingAverage?'checked':''}><span>Media 7 gg</span></label></div><div class="tabs">${[[7,'7 giorni'],[30,'30 giorni'],[90,'3 mesi'],[0,'Tutto']].map(([n,l])=>`<button data-trend-days="${n}" class="${state.trendDays===n?'active':''}">${l}</button>`).join('')}</div>${chartHtml(all,state.trendDays,state.showMovingAverage)}</section><section class="card summary"><div class="section-head"><h2>Riepilogo peso</h2><span class="pill">Totale</span></div>${first?`<div class="stats"><div><span>Peso iniziale</span><b>${kg(first.weight)}</b></div><div><span>Ultimo peso</span><b>${kg(last.weight)}</b></div><div><span>Variazione</span><b>${delta>0?'+':''}${delta.toFixed(1).replace('.',',')} kg</b></div></div>`:'<p class="muted">Nessun peso registrato.</p>'}</section><section class="card"><div class="section-head"><h2>Storico</h2><span class="pill">${rows.length}</span></div>${rows.map(d=>`<div class="listitem" data-edit-diary="${d.id}"><span><b>${fmtDate(d.entry_date)}</b><small>${esc([d.breakfast,d.lunch,d.dinner].filter(Boolean).join(' · ')||'Giornata registrata')}</small></span><div class="history-right"><b>${kg(d.weight_kg)}</b>${d.water!=null?`<small>💧 ${Number(d.water).toFixed(1).replace('.',',')} L</small>`:''}</div></div>`).join('')||'<p class="muted">Nessuna giornata registrata.</p>'}</section>`;}

  function measurementsHtml(){const diaryByDate=new Map(state.diary.filter(x=>x.weight_kg!=null).map(x=>[x.entry_date,x])),selfRows=state.selfMeasures.map(m=>({...m,patientWeight:diaryByDate.get(m.measured_at)?.weight_kg??null})).sort((a,b)=>b.measured_at.localeCompare(a.measured_at)),proRows=[...state.proMeasures].sort((a,b)=>b.measured_at.localeCompare(a.measured_at));return `<div class="hero-title"><div><div class="eyebrow">EVOLUZIONE CORPOREA</div><h1>Misure</h1></div></div>${active()?`<section class="card"><div class="section-head"><h2>Automisurazione</h2><span class="pill">Inserita da te</span></div><p class="muted">Il peso viene dal Diario. Qui puoi registrare vita e fianchi misurati autonomamente.</p><label>Data</label><input id="selfMeasureDate" type="date" value="${isoToday()}"><div class="measure-grid"><div><label>Vita (cm)</label><input id="selfMeasureWaist" inputmode="decimal"></div><div><label>Fianchi (cm)</label><input id="selfMeasureHips" inputmode="decimal"></div></div><label>Note</label><textarea id="selfMeasureNotes"></textarea><button class="primary" id="saveSelfMeasure">Salva automisurazione</button></section>`:''}<section class="card"><div class="section-head"><h2>Le tue automisurazioni</h2><span class="pill">Paziente</span></div>${selfRows.length?`<div class="measure-table-wrap"><table class="measure-table"><thead><tr><th>Data</th><th>Peso diario</th><th>Vita</th><th>Fianchi</th><th>Note</th></tr></thead><tbody>${selfRows.map(m=>`<tr data-edit-self-measure="${m.id}"><td>${fmtDate(m.measured_at)}</td><td>${kg(m.patientWeight)}</td><td>${cm(m.waist_cm)}</td><td>${cm(m.hips_cm)}</td><td>${esc(m.notes||'')}</td></tr>`).join('')}</tbody></table></div>`:'<p class="muted">Nessuna automisurazione.</p>'}</section><section class="card"><div class="section-head"><h2>Misure rilevate dal professionista</h2><span class="pill">Professionista</span></div>${proRows.length?`<div class="measure-table-wrap"><table class="measure-table"><thead><tr><th>Data</th><th>Peso</th><th>Vita</th><th>Fianchi</th><th>Note</th></tr></thead><tbody>${proRows.map(m=>`<tr><td>${fmtDate(m.measured_at)}</td><td>${kg(m.weight_kg)}</td><td>${cm(m.waist_cm)}</td><td>${cm(m.hips_cm)}</td><td>${esc(m.notes||'')}</td></tr>`).join('')}</tbody></table></div>`:'<p class="muted">Nessuna misura rilevata dal professionista.</p>'}</section>`;}

  function documentsHtml(){const planDocIds=new Set(state.planLinks.map(x=>x.document_id)),accounting=state.documents.filter(d=>['accounting','invoice'].includes(d.category)),blood=state.documents.filter(d=>d.sub_category==='blood_test'),health=state.documents.filter(d=>d.category==='health'&&d.sub_category!=='blood_test'),professionalDocs=state.documents.filter(d=>d.category==='document'&&!planDocIds.has(d.id));const row=d=>`<div class="document-row"><div><b>${esc(d.title)}</b><span>${fmtDate(d.document_date||d.created_at)} · ${esc(d.original_filename)}</span>${d.professional_note?`<small>${esc(d.professional_note)}</small>`:''}</div><button class="secondary compact" data-open-doc="${d.id}">Apri</button></div>`;return `<div class="page-title"><button class="back" data-go="home">‹</button><div><div class="eyebrow">ARCHIVIO PAZIENTE</div><h1>Documenti</h1></div></div><section class="card"><div class="section-head"><h2>Piani alimentari</h2><span class="pill">${state.plans.length}</span></div>${state.plans.map(p=>{const link=state.planLinks.find(x=>x.nutrition_plan_id===p.id),doc=link&&state.documents.find(d=>d.id===link.document_id);return `<div class="document-row"><div><b>${esc(p.title)}${p.status==='active'?'<span class="plan-status-badge">IN VIGORE</span>':''}</b><span>${p.valid_from?'Valido dal '+fmtDate(p.valid_from):'Decorrenza non indicata'}</span></div>${doc?`<button class="secondary compact" data-open-doc="${doc.id}">Apri</button>`:''}</div>`;}).join('')||'<p class="muted">Nessun piano pubblicato.</p>'}</section><section class="card"><div class="section-head"><h2>Documenti del professionista</h2><span class="pill">${professionalDocs.length}</span></div>${professionalDocs.map(row).join('')||'<p class="muted">Nessun documento pubblicato dal professionista.</p>'}</section><section class="card"><div class="section-head"><h2>Documenti contabili</h2><span class="pill">${accounting.length}</span></div>${accounting.map(row).join('')||'<p class="muted">Nessun documento contabile.</p>'}</section><section class="card"><div class="section-head"><h2>Analisi del sangue</h2><span class="pill">${blood.length}</span></div>${active()?'<button class="primary" data-upload-kind="blood_test">＋ Carica analisi</button>':''}${blood.map(row).join('')||'<p class="muted">Nessun referto analisi.</p>'}</section><section class="card"><div class="section-head"><h2>Altri documenti sanitari</h2><span class="pill">${health.length}</span></div>${active()?'<button class="secondary" data-upload-kind="health_other">＋ Carica documento</button>':''}${health.map(row).join('')||'<p class="muted">Nessun altro documento sanitario.</p>'}</section><input id="patientUploadFile" type="file" accept=".pdf,application/pdf,image/*" hidden>`;}

  function profileHtml(){const c=state.context,p=c.patient,cl=c.clinical||{},visit=nextAppointment(),acceptedIds=new Set(state.privacy.acceptances.map(a=>a.privacy_document_id)),sex=p.sex==='M'?'Maschile':p.sex==='F'?'Femminile':p.sex||'—';return `<div class="page-title"><button class="back" data-go="home">‹</button><div><div class="eyebrow">DATI PERSONALI</div><h1>Profilo</h1></div></div><section class="card"><div class="section-head"><h2>Profilo gestito dal professionista</h2></div><p class="muted">I dati anagrafici e clinici sono gestiti dal professionista. Se qualcosa non è corretto, segnalalo durante la visita.</p></section><section class="card"><h2>Dati personali</h2><div class="profile-read-grid"><div><span>Nome</span><b>${esc(c.profile.first_name)}</b></div><div><span>Cognome</span><b>${esc(c.profile.last_name)}</b></div><div><span>Data di nascita</span><b>${fmtDate(p.birth_date)}</b></div><div><span>Sesso</span><b>${esc(sex)}</b></div><div><span>Altezza</span><b>${p.height_cm?cm(p.height_cm):'—'}</b></div><div><span>Peso obiettivo</span><b>${kg(cl.goal_weight_kg)}</b></div><div><span>Prossima visita</span><b>${visit?new Date(visit.starts_at).toLocaleString('it-IT',{dateStyle:'short',timeStyle:'short'}):'—'}</b></div></div></section><section class="card"><h2>Storia del peso</h2><div class="profile-read-grid"><div><span>Peso minimo storico</span><b>${kg(cl.min_weight_kg)}</b></div><div><span>Peso massimo storico</span><b>${kg(cl.max_weight_kg)}</b></div><div><span>Peso ragionevole / concordato</span><b>${kg(cl.reasonable_weight_kg)}</b></div></div></section><details class="patient-section"><summary>Anamnesi e obiettivi</summary><div class="profile-read-grid section-body"><div><span>Diagnosi / motivo</span><b>${esc(cl.diagnosis||'—')}</b></div><div><span>Attività lavorativa</span><b>${esc(cl.work||'—')}</b></div><div><span>Attività fisica</span><b>${esc(cl.activity||'—')}</b></div><div><span>Fumo</span><b>${esc(cl.smoking||'—')}</b></div><div><span>Alcol</span><b>${esc(cl.alcohol||'—')}</b></div><div><span>Allergie / intolleranze</span><b>${esc(cl.allergies||'—')}</b></div><div><span>Farmaci</span><b>${esc(cl.medications||'—')}</b></div><div><span>Osservazioni</span><b>${esc(cl.observations||'—')}</b></div><div><span>Obiettivi</span><b>${esc(cl.objectives||'—')}</b></div></div></details><section class="card"><div class="section-head"><h2>Privacy</h2><span class="pill">${state.privacy.documents.length}</span></div>${state.privacy.documents.map(d=>`<div class="document-row"><div><b>${esc(d.title)}</b><span>Versione ${esc(d.version)} · ${acceptedIds.has(d.id)?'Accettata':'Da accettare'}</span></div>${acceptedIds.has(d.id)?'<span class="pill">OK</span>':`<button class="primary compact" data-accept-privacy="${d.id}">Accetta</button>`}</div>`).join('')||'<p class="muted">Nessuna informativa attiva.</p>'}</section><section class="card support-entry-card"><div><div class="eyebrow">SUPPORTO TECNICO</div><h2>Assistenza NUBEMO</h2><p class="muted">Per problemi tecnici puoi preparare una mail all’assistenza.</p></div><button class="secondary" id="patientSupport">Segnala un problema</button></section>`;}

  function render(){document.body.dataset.page=state.page;document.querySelectorAll('nav [data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page===state.page));app.innerHTML=state.page==='home'?homeHtml():state.page==='add'?diaryFormHtml():state.page==='trend'?trendHtml():state.page==='measures'?measurementsHtml():state.page==='documents'?documentsHtml():profileHtml();}
  function goToPage(page){state.page=page;if(page==='add'){const today=state.diary.find(x=>x.entry_date===isoToday());state.editDiaryId=today?.id||null;}else state.editDiaryId=null;render();scrollTo(0,0);}
  async function refreshAndRender(){loading();await reloadAll();render();}

  async function saveDiary(){try{const values={entry_date:document.getElementById('diaryDate').value,weight_kg:nullableNumber(document.getElementById('diaryWeight').value),water:nullableNumber(document.getElementById('diaryWater').value),coffee:Number(document.getElementById('diaryCoffee').value||0),sweetener:textOrNull(document.getElementById('diarySweetener').value),breakfast:textOrNull(document.getElementById('diaryBreakfast').value),morning_snack:textOrNull(document.getElementById('diaryMorningSnack').value),lunch:textOrNull(document.getElementById('diaryLunch').value),afternoon_snack:textOrNull(document.getElementById('diaryAfternoonSnack').value),dinner:textOrNull(document.getElementById('diaryDinner').value),sport:textOrNull(document.getElementById('diarySport').value),notes:textOrNull(document.getElementById('diaryNotes').value)};if(!values.entry_date)return alert('Seleziona una data.');if(values.water!=null&&(values.water<0||values.water>20))return alert('Controlla la quantità di acqua.');await services.saveDiaryEntry(state.context.patient.id,state.context.user.id,values,state.editDiaryId);state.editDiaryId=null;state.page='home';await refreshAndRender();}catch(e){if(e.message?.includes('duplicate')||e.message?.includes('unique'))alert('Questa data è già registrata.');else errorMessage(e);}}
  async function saveSelfMeasure(){try{const values={measured_at:document.getElementById('selfMeasureDate').value,waist_cm:nullableNumber(document.getElementById('selfMeasureWaist').value),hips_cm:nullableNumber(document.getElementById('selfMeasureHips').value),notes:textOrNull(document.getElementById('selfMeasureNotes').value)};if(!values.measured_at)return alert('Inserisci la data.');if(values.waist_cm!=null&&(values.waist_cm<20||values.waist_cm>300))return alert('Controlla la circonferenza vita.');if(values.hips_cm!=null&&(values.hips_cm<20||values.hips_cm>300))return alert('Controlla la circonferenza fianchi.');await services.saveSelfMeasurement(state.context.patient.id,state.context.user.id,values,state.editSelfMeasureId);state.editSelfMeasureId=null;await refreshAndRender();}catch(e){if(e.message?.includes('duplicate')||e.message?.includes('unique'))alert('Esiste già una automisurazione per questa data.');else errorMessage(e);}}
  async function uploadDocument(kind,file){const title=prompt('Titolo documento',file.name.replace(/\.[^.]+$/,''));if(!title)return;const date=prompt('Data documento (AAAA-MM-GG)',isoToday());if(date===null)return;try{loading('Caricamento documento…');await services.uploadPatientDocument(state.context.patient.id,state.context.user.id,file,{category:'health',sub_category:kind,title:title.trim(),document_date:date||null});await refreshAndRender();}catch(e){errorMessage(e);render();}}
  function exportDiaryPdf(){if(!state.diary.length)return alert('Nessuna giornata da esportare.');const rows=state.diary.map(d=>[fmtDate(d.entry_date),d.weight_kg==null?'':Number(d.weight_kg).toFixed(1).replace('.',','),d.water==null?'':Number(d.water).toFixed(1).replace('.',','),d.coffee??'',d.sweetener||'',d.breakfast||'',d.morning_snack||'',d.lunch||'',d.afternoon_snack||'',d.dinner||'',[d.sport,d.notes].filter(Boolean).join(' · ')]);const blob=window.NubemoDiaryPdf.create({rows,origin:'patient'}),url=URL.createObjectURL(blob);window.location.href=url;setTimeout(()=>URL.revokeObjectURL(url),120000);}

  app.addEventListener('click',async e=>{const go=e.target.closest('[data-go]');if(go){goToPage(go.dataset.go);return;}const edit=e.target.closest('[data-edit-diary]');if(edit&&active()){state.editDiaryId=edit.dataset.editDiary;state.page='add';render();return;}const td=e.target.closest('[data-trend-days]');if(td){state.trendDays=Number(td.dataset.trendDays);render();return;}if(e.target.id==='saveDiary')return saveDiary();if(e.target.id==='deleteDiary'){if(confirm('Eliminare questa giornata?')){try{await services.deleteDiaryEntry(state.editDiaryId);state.editDiaryId=null;state.page='home';await refreshAndRender();}catch(err){errorMessage(err);}}return;}if(e.target.id==='duplicateDiary'){const src=state.diary.find(x=>x.id===state.editDiaryId);state.editDiaryId=null;render();setTimeout(()=>{const ids=['diaryWeight','diaryWater','diaryCoffee','diarySweetener','diaryBreakfast','diaryMorningSnack','diaryLunch','diaryAfternoonSnack','diaryDinner','diarySport','diaryNotes'],vals=[null,src.water,src.coffee,src.sweetener,src.breakfast,src.morning_snack,src.lunch,src.afternoon_snack,src.dinner,src.sport,src.notes];document.getElementById('diaryDate').value=isoToday();ids.forEach((id,i)=>{const el=document.getElementById(id);if(el)el.value=vals[i]??'';});},0);return;}if(e.target.id==='saveSelfMeasure')return saveSelfMeasure();const open=e.target.closest('[data-open-doc]');if(open){const doc=state.documents.find(d=>d.id===open.dataset.openDoc);if(doc){try{const url=await services.openDocument(doc);if(url)window.location.href=url;}catch(err){errorMessage(err);}}return;}const upload=e.target.closest('[data-upload-kind]');if(upload){const input=document.getElementById('patientUploadFile');input.dataset.kind=upload.dataset.uploadKind;input.click();return;}const accept=e.target.closest('[data-accept-privacy]');if(accept){try{await services.acceptPrivacy(state.context.profile.id,accept.dataset.acceptPrivacy);await refreshAndRender();}catch(err){errorMessage(err);}return;}if(e.target.id==='exportDiaryPdf')return exportDiaryPdf();if(e.target.id==='patientSupport'){const subject='NUBEMO - Segnalazione problema Area Paziente';window.location.href=`mailto:giov.cavaliere@gmail.com?subject=${encodeURIComponent(subject)}`;}});
  app.addEventListener('change',e=>{if(e.target.id==='patientUploadFile'&&e.target.files?.[0])uploadDocument(e.target.dataset.kind||'health_other',e.target.files[0]);if(e.target.id==='movingAverageToggle'){state.showMovingAverage=e.target.checked;render();}});
  document.querySelector('nav')?.addEventListener('click',e=>{const b=e.target.closest('[data-page]');if(!b)return;goToPage(b.dataset.page);});
  window.nubemoPatientApp={async init(context){state.context=context;loading();await reloadAll();render();},state};
})();
